<%= form_for(@beer) do |f| %>
  <% if @beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
      <% @beer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :abv %><br>
    <%= f.text_field :abv %>
  </div>
  <div class="field">
    <%= f.label :malt_id %><br>
    <%= f.select :malt_id, @options_malt %>
  </div>
  <div class="field">
    <%= f.label :hop_id %><br>
    <%= f.select :hop_id, @options_hop%>
  </div>
  <div class="field">
    <%= f.label :yeast_id %><br>
    <%= f.select :yeast_id, @options_yeast %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
